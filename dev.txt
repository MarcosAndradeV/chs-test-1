macro_rules! assemble {
    (push $a:expr) => {
        {
            let v: CHSValue = CHSValue::I($a);
            Instr::new(Opcode::Push, Some(v))
        }
    };
}

proc main {
    1 1 +
}

main:
    push 1
    print
    halt